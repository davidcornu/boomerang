#!/usr/bin/env node

var boomerang = require('../lib/boomerang');
var options   = { port: process.env.PORT };
boomerang.createServer(options);
console.log("Boomerang listening on port " + options.port);